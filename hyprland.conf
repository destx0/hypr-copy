
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#
# XDG_CURRENT_DESKTOP=Hyprland
autogenerated = 0 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=eDP-1,preferred,auto,auto
monitor=,highres,auto,1.15
# monitor=HDMI-A-1,disables
# monitor=,preferred,auto,1
# monitor=HDMI-A-1,1920x1080,1920x0,1


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = waybar & hyprpaper & vivaldi-stable & hypr-empty & copyq
exec-once = hypr-empty 
exec-once = waybg -i /home/voy/Downloads/boydog.jpg 
exec-once = /home/voy/.config/waybar/powernot.sh
exec-once = /home/voy/.config/hypr/cngwallp.sh

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24
env = WLR_NO_HARDWARE_CURSORS,1
# GDK_BACKEND=wayland,x11

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant=
    kb_model =
    kb_options =
    kb_rules =
    numlock_by_default = true
    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

# general {
#     # See https://wiki.hyprland.org/Configuring/Variables/ for more

#     gaps_in = 5
#     gaps_out = 5
#     border_size = 5
#     #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
#     col.active_border = rgb(9580ff) rgb(ff79c6) 45deg
#     col.inactive_border = rgba(595959aa)

#     layout = dwindle
# }
general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 4
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.active_border = rgb(eaac8b)

    # gradient border
    # col.active_border = rgb(9580ff) rgb(ff79c6) 45deg


    # col.active_border = rgb(9580ff) rgb(ff79c6) 45deg
    col.inactive_border = rgb(646a75)

    layout = dwindle
}


# decoration {
#     # See https://wiki.hyprland.org/Configuring/Variables/ for more

#     rounding = 10
#     blur = yes
#     blur_size = 3
#     blur_passes = 3
#     blur_new_optimizations = on
#     #inactive_opactity = 0.5
#     drop_shadow = yes
#     shadow_range = 4
#     shadow_render_power = 3
#     col.shadow = rgba(1a1a1aee)
# }
decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    blur = yes
    blur_size = 30
    blur_passes = 2
    blur_ignore_opacity = true
    # active_opacity = 0.90
    # inactive_opacity = 0.80
    blur_new_optimizations = on
    #inactive_opactity = 0.5
    # drop_shadow = yes
    shadow_range = 20
    shadow_render_power = 5000
    # col.shadow = rgba(1a1a1aee)
    col.shadow = rgb(eaac8b)
}

# animations {
#     enabled = yes

#     # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

#     bezier = myBezier, 0.05, 0.9, 0.1, 1.05

#     animation = windows, 1, 7, myBezier
#     animation = windowsOut, 1, 7, default, popin 80%
#     animation = border, 1, 10, default
#     animation = borderangle, 1, 8, default
#     animation = fade, 1, 7, default
#     # animation = workspaces, 1, 6, default
#     animation = workspaces, 1, 3, default
# }
animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 4, myBezier
    animation = windowsOut, 1, 4, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    # animation = workspaces, 1, 6, default
    animation = workspaces, 1, 3, myBezier
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    workspace_swipe = on
}
binds {
    workspace_back_and_forth = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5 #https://wiki.hyprland.org/Configuring/Variables/
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, kitty
bind = $mainMod, Q, killactive, 
bind = $mainMod SHIFT, E, exit, 
bind = $mainMod, E, exec, dolphin
bind = $mainMod SHIFT, R, exec, reboot
bind = $mainMod, V, togglefloating, 
bind = $mainMod, SPACE, exec, wofi --show drun -I
# bind = $mainMod, SPACE, exec, (hyprctl clients | grep "class: " | awk '{gsub("class: ", "");print}' | wofi --show dmenu -I)
bind = $mainMod SHIFT, SPACE, exec, wofi --show run -I
# bind = $mainMod, A, exec, hyprctl clients | awk '/title: ./ { gsub("\t*title: *", ""); print}' | rofi -dmenu | xargs -I{} hyprctl dispatch focuswindow "title:{}"
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, 
bind = $mainMod, G, togglegroup, 
bind = $mainMod, H, moveintogroup, 
bind = ALT, TAB, changegroupactive, f


# bind = $mainMod, B, exec, vivaldi-stable
bind = $mainMod, B, exec, /home/voy/.config/waybar/openvivaldi.sh
bind = $mainMod, N, exec, /home/voy/.config/waybar/opennotion.sh
bind = $mainMod, C, exec, /home/voy/.config/waybar/opencode.sh
bind = $mainMod SHIFT, C, exec, /home/voy/.config/waybar/opencodium.sh
bind = $mainMod SHIFT, B, exec, /home/voy/.config/waybar/openfirefox.sh
bind = $mainMod, F, exec, /home/voy/.config/waybar/openfileman.sh
bind = $mainMod SHIFT, D, exec, /home/voy/.config/hypr/ytdwnld.sh

bind = $mainMod , S, exec, budgie-control-center 
bind = $mainMod , D, exec, evince

# bind = $mainMod, F, exec, nautilusffffff
# bind = $mainMod, C, exec, code
# bind=,SHIFT,exec,/path/to/hyprland-interactive-screenshot

# bind = $mainMod, L, exec, sudo light -S 10


# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
# bind = $mainMod, C, focuswindow, ^(Code)$

bind = $mainMod SHIFT, right, resizeactive, 30 0
bind = $mainMod SHIFT, left, resizeactive, -30 0
bind = $mainMod SHIFT, up, resizeactive, 0 -10
bind = $mainMod SHIFT, down, resizeactive, 0 10

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


bind = $mainMod, M, fullscreen,1
bind = $mainMod SHIFT, M, fullscreen,1

bind = ,code:76, exec,copyq toggle


bind = ,PRINT, exec,wl-copy < $(grimshot --notify save  )    
bind = SHIFT, PRINT, exec,wl-copy < $(grimshot --notify save area )    


bind = $mainMod, TAB, workspace,e+1
bind = $mainMod SHIFT, TAB, workspace,e-1

bind = ,code:232, exec, brightnessctl -c backlight set 5%-
bind = ,code:233, exec, brightnessctl -c backlight set 5%+
bind = ,code:121, exec, pamixer -t
bind = ,code:122, exec, pamixer -d 5
bind = ,code:123, exec, pamixer -i 5
bind = ,Insert, exec, /home/voy/.config/hypr/noti.sh
# bind = ,code:157, exec, killall waybar
bind = ,code:157, exec, /home/voy/.config/waybar/hidebar.sh
bind = ,code:148, exec, /home/voy/.config/hypr/cngwallonce.sh
bind = ,Prior,  exec, hyprctl dispatch moveworkspacetomonitor $(hyprctl activewindow | grep "workspace:" | awk '{print  $2}')  $(hyprctl activewindow | grep "monitor:" | awk '{print ($2 == 1 ? 0 : 1)}') 
# bind = , Next , dpms , on   HDMI-A-1 
# bind = $mainMod, TAB, exec, hyprctl clients | grep "class: " | awk '{gsub("class: ", "");print}' | wofi --show  dmenu 

# bind = SUPER,, exec, killall waybar 

windowrule=float,^(com.github.hluk.copyq)$